# NOTE networks had to be prepended with letters to ensure 
# proper alphabetical order 

services:
  simulation:
    image: fortiphyd/grfics-simulation
    container_name: simulation
    hostname: simulation
    build: ./simulation
    cap_add:
      - NET_ADMIN
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "55555"]
      interval: 5s
      retries: 5
    networks:
      a-grfics-admin:  # gets random bridge IP (e.g., 172.18.x.x)
      b-ics-net:
        ipv4_address: 192.168.95.10

    # Optional if you want to override default CMD
    # command: sh -c "./simulation/simulation & cd modbus && ./run_all.sh"

  plc:
    image: fortiphyd/grfics-plc
    container_name: plc
    hostname: plc
    build: ./plc
    cap_add:
      - NET_ADMIN
    ports:
      - "8080:8080"
    networks:
      a-grfics-admin:  # gets random bridge IP (e.g., 172.18.x.x)
      b-ics-net:
        ipv4_address: 192.168.95.2
    volumes:
      - plc_volume:/docker_persistent

    # Optional if you want to override default CMD
    # command: sh -c "./simulation/simulation & cd modbus && ./run_all.sh"


  ews:
    image: fortiphyd/grfics-workstation
    container_name: EWS
    hostname: EWS
    build: ./workstation
    cap_add:
      - NET_ADMIN
    ports:
      - "6080:6080"
    networks:
      a-grfics-admin:  # gets random bridge IP (e.g., 172.18.x.x)
      b-ics-net:
        ipv4_address: 192.168.95.5

    # Optional if you want to override default CMD
    # command: sh -c "./simulation/simulation & cd modbus && ./run_all.sh"

  hmi:
    image: fortiphyd/grfics-scadalts
    container_name: HMI
    build: ./scadalts
    dns:
     - 8.8.8.8
     - 1.1.1.1
    cap_add:
      - NET_ADMIN
    ports:
      - "6081:8080"
    networks:
      a-grfics-admin:  # gets random bridge IP (e.g., 172.18.x.x)
      c-dmz-net:
        ipv4_address: 192.168.90.107
    volumes:
      - scadalts_db:/var/lib/mysql
    # Optional if you want to override default CMD
    # command: sh -c "./simulation/simulation & cd modbus && ./run_all.sh"

  kali:
    image: fortiphyd/grfics-attacker
    container_name: kali
    hostname: kali
    build: ./attacker
    cap_add:
      - NET_ADMIN
    ports:
      - "6088:6080"
    networks:
      a-grfics-admin:  # gets random bridge IP (e.g., 172.18.x.x)
      c-dmz-net:
        ipv4_address: 192.168.90.6
    sysctls:
      net.ipv4.conf.default.arp_announce: 2
      net.ipv4.conf.all.arp_announce: 2

  # Simulated Level 4 / enterprise entry point
  l4-web:
    image: fortiphyd/grfics-l4-web
    container_name: l4-web
    hostname: l4-web
    build: ./l4-web
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    ports:
      - "8085:80"
    environment:
      # Read-only polling against HMI-side HTTP status endpoints.
      - HMI_BASE_URL=http://192.168.90.107:8080
      - HMI_LOGIN_PATH=/login.htm
      - HMI_STATUS_URLS=http://192.168.90.107:8080/api/status,http://192.168.90.107:8080/status
      - HMI_POLL_INTERVAL_SECONDS=10
      - HMI_USERNAME=admin
      - HMI_PASSWORD=admin
    depends_on:
      - hmi
    networks:
      a-grfics-admin:
      d-l4-net:
        ipv4_address: 192.168.100.10

  router:
    image: fortiphyd/grfics-router
    container_name: router
    hostname: router
    build: ./router
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "bash", "-c", "ip route | grep -q 192.168.95.0 || exit 1"]
      interval: 5s
      retries: 5
    cap_add:
     - NET_ADMIN
     - NET_RAW
    sysctls:
      net.ipv4.ip_forward: '1'
    environment:
      - FWUI_SECRET_KEY=some-long-secret-you-generate
    volumes:
     - router_config:/etc/firewall
    networks:
      a-grfics-admin:
      b-ics-net:
        ipv4_address: 192.168.95.200
      c-dmz-net:
        ipv4_address: 192.168.90.200
      d-l4-net:
        ipv4_address: 192.168.100.200

  caldera:
    image: fortiphyd/grfics-caldera
    container_name: caldera
    hostname: caldera
    build: ./caldera
    restart: unless-stopped
    ports:
      - "8888:8888"
    networks:
      a-grfics-admin:
      c-dmz-net:
        ipv4_address: 192.168.90.250

networks:
  b-ics-net:
    driver: macvlan
    driver_opts:
      parent: ${GRFICS_PARENT_IFACE:-eth0}  # Set in .env if host NIC is not eth0 (e.g., ens4 on GCE)
    ipam:
      config:
        - subnet: 192.168.95.0/24
          gateway: 192.168.95.1

  c-dmz-net:
    driver: macvlan
    driver_opts:
      parent: ${GRFICS_PARENT_IFACE:-eth0}  # Set in .env if host NIC is not eth0 (e.g., ens4 on GCE)
    ipam:
      config:
        - subnet: 192.168.90.0/24
          gateway: 192.168.90.1

  # Level 4 / enterprise subnet for WAN-side demo access
  d-l4-net:
    driver: macvlan
    driver_opts:
      parent: ${GRFICS_PARENT_IFACE:-eth0}  # Set in .env if host NIC is not eth0 (e.g., ens4 on GCE)
    ipam:
      config:
        - subnet: 192.168.100.0/24
          gateway: 192.168.100.1

  a-grfics-admin:
    driver: bridge

volumes:
  scadalts_db:
    name: scadalts_db
  plc_volume:
    name: plc_volume
  router_config:
    name: router_config
